import { app } from "./app";
import { pos } from "./pos";
import { shell } from "./shell";
import { theme } from "./theme";
import { window } from "./window";

export const router = {
  theme,
  window,
  app,
  shell,
  pos,
};
